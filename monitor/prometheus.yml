# Prometheus scrape config for brokemode.
#   prometheus --config.file=monitor/prometheus.yml
global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: brokemode
    static_configs:
      - targets: ["127.0.0.1:9101"]
        labels:
          host: macbook-m2
